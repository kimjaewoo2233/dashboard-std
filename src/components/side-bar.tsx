import clsx from "clsx"


const Sidebar = () => {

    return (
        <aside className={clsx("bg-[#FFF] px-[39px] fixed left-0 top-0 flex h-screen transition-all duration-300 w-[300px] z-20")}>
            test
        </aside>
    )
}

export default Sidebar;
